name: publish

on:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - name: install deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: 2.x
      - run: deno publish
